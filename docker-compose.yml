version: '1'
services:
  petshop-app:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    environment:
      - spring.datasource.url=jdbc:oracle:thin:@mibd_tp?TNS_ADMIN=/app/oracle_wallet
      - spring.datasource.username=FULLSTACK_1
      - spring.datasource.password=Password.123
    restart: always